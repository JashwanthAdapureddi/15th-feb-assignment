values = [2, 3, 5, 7, 11] 

# Multiply each element of the values list by 5.
for i in range(0,len(values)):
   values[i] = values[i] * 5
   for i in range(0,len(values)):
   values[i] = values[i] * 5
